#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import io

def patch(path, pairs):
    txt = io.open(path, encoding="utf-8").read()
    for old, new in pairs:
        assert old in txt, (path, old[:40])
        txt = txt.replace(old, new, 1)
    io.open(path, "w", encoding="utf-8", newline="\n").write(txt)
    print("patched", path, "+%d" % sum(len(n) - len(o) for o, n in pairs))

patch("docs/book/13-array.md", [
    ("| 选型 | 同型高频遍历 → 值数组；多态/归属别处 → 指针数组 |",
     "| 选型 | 同型高频遍历 → 值数组；多态/归属别处 → 指针数组；键值查找 → 第 17 章 map |"),
])

patch("docs/book/14-buffer.md", [
    ("### 基础：转储复现\n\n按主示例手工构造 `61 62 63 00 00 7a`：",
     "### 基础：转储复现\n\n先合上书在纸上写出六个字节的预期值，再按主示例手工构造 `61 62 63 00 00 7a`："),
    ("### 进阶：TLV 解码器骨架\n\n用 `xbuffer` 实现最小 TLV（类型-长度-值）累积器：",
     "### 进阶：TLV 解码器骨架（协议世界的第一个玩具）\n\n用 `xbuffer` 实现最小 TLV（类型-长度-值）累积器："),
    ("- **容量纪律**：已知量级先 `Reserve`；长跑驻留前 `Trim`。",
     "- **容量纪律**：已知量级先 `Reserve`；长跑驻留前 `Trim`；Trim 只砍空闲空间，不动已写内容。"),
])

patch("docs/book/15-stack.md", [
    ("### 基础：LIFO 三帧\n\n复现主示例：",
     "### 基础：LIFO 三帧\n\n复现主示例（先手写预期输出再运行对拍）："),
    ("- **地址时效**：动态栈扩容失效；固定栈绑定期间稳定；分块栈永不失效。",
     "- **地址时效**：动态栈扩容失效；固定栈绑定期间稳定；分块栈永不失效——选型时先问\"栈元素会不会被栈外引用\"。"),
    ("④ 分块栈永不失效",
     "④ 分块栈永不失效（增长挂新块）"),
    ("| 四变体 | 动态（通用）/ 固定（零分配）/ 分块（稳定地址）/ 指针（装引用） |",
     "| 四变体 | 动态 `xstack`（通用）/ 固定 `xfixedstack`（零分配）/ 分块 `xblockstack`（稳定地址）/ 指针栈（装引用） |"),
    ("## 概念\n\n### 四个变体一张表",
     "## 概念\n\n先给出全章的骨架：一张选型表、一条\"帧栈的一生\"图示、两份契约（零分配与稳定地址）、一个 LIFO 错误卫生的日常习惯。读完概念区，你应该已经能回答本章开头场景题——这正是\"约束驱动选型\"的第一次实操。\n\n### 四个变体一张表"),
])

patch("docs/book/16-list-slotmap.md", [
    ("### 基础：链序复现\n\n复现主示例两行输出；",
     "### 基础：链序复现\n\n复现主示例两行输出（先手推再运行）；"),
    ("- **代际语义**：槽复用代际 +1；句柄校验\"下标 + 代际\"双匹配；`XRT_SLOT_INVALID` 为无效哨兵。",
     "- **代际语义**：槽复用代际 +1；句柄校验\"下标 + 代际\"双匹配；`XRT_SLOT_INVALID` 为无效哨兵；拆开句柄只取下标等于放弃保护。"),
])
print("done")

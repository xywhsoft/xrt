#!/usr/bin/env python3
# -*- coding: utf-8 -*-
import io

def patch(path, pairs):
    txt = io.open(path, encoding="utf-8").read()
    for old, new in pairs:
        assert old in txt, (path, old[:36])
        txt = txt.replace(old, new, 1)
    io.open(path, "w", encoding="utf-8", newline="\n").write(txt)
    print("ok", path)

patch("docs/book/18-map.md", [
    ("### 基础：词频统计\n\n读入一段英文（硬编码字符串即可），按单词为键统计词频，按插入序输出全部词条。提示：空格分词、`GetOrAdd` 计数。",
     "### 基础：词频统计\n\n读入一段英文（硬编码字符串即可），按单词为键统计词频，按插入序输出全部词条。提示：空格分词、`GetOrAdd` 计数；输出前先在纸上写出预期的词条顺序（插入序），再与程序对拍。"),
    ("- **键的二进制安全**：字节键可含内嵌零；长度以视图为准，不看零结尾。",
     "- **键的二进制安全**：字节键可含内嵌零；长度以视图为准，不看零结尾。\n- **宿主形态**：临时统计（栈句柄）、长活表（挂服务对象）、键到句柄（值装 `ptr`）三种形态各有收尾纪律。"),
])

patch("docs/book/19-set.md", [
    ("### 基础：去重计数\n\n把一组含重复的 ID 装进集合，输出 `Count` 与去重后的元素列表；再与第 14 章数组的\"排序去重\"做法对比代码量。",
     "### 基础：去重计数\n\n把一组含重复的 ID 装进集合，输出 `Count` 与去重后的元素列表；再与第 14 章数组的\"排序去重\"做法对比代码量与输出顺序（数组法输出有序、集合法输出插入序——两种顺序各自的服务场景想一句）。"),
    ("- **迭代纪律**：与 `xmap` 相同——三段式、迭代期指针不出迭代期、迭代中禁增删。",
     "- **迭代纪律**：与 `xmap` 相同——三段式、迭代期指针不出迭代期、迭代中禁增删。\n- **工程落点**：白名单/标签分组/状态迁移表——集合代码里出现的是业务词汇本身。"),
])

patch("docs/book/20-avl.md", [
    ("### 基础：乱序插入验证\n\n复现主示例：乱序插入 5 个会话，断言中序输出恒为升序；再按另一种插入顺序重跑，输出不变。",
     "### 基础：乱序插入验证\n\n复现主示例：乱序插入 5 个会话，断言中序输出恒为升序；再按另一种插入顺序重跑，输出不变——两次运行之间树内部形状可能完全不同，但中序输出一致，这正是\"平衡是树的内部事务\"的可观察证据。"),
    ("- **挂树纪律**：`NodeInit` 先行；摘树先于销毁对象。",
     "- **挂树纪律**：`NodeInit` 先行；摘树先于销毁对象；对象生命周期覆盖挂树期（与第 17 章链表同纪律）。\n- **三层结构**：存储（池/slot_map）+ 定位（map）+ 有序视图（侵入式 AVL）——真实资源管理器的标配。"),
])
print("done")

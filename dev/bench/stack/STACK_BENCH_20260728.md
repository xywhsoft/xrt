# Stack 性能基线（2026-07-28）

## 范围

- 模块化独立翻译单元构建，不启用 LTO；
- 预留路径：`100000` 轮，每轮容量 `64`，共 `12800000` 次操作；
- 增长路径：从空栈连续增加 `1000000` 个 `uint64`；
- 最终容量：`1347968`；
- checksum：`820197900000`。

## GCC x64

编译器：`w64devkit gcc`，参数：`-O3 -std=c11 -m64`。

| 路径 | 耗时 | 吞吐 |
|---|---:|---:|
| 预留 `Push/Pop` | 84,845,000 ns | 150,863,339.030 ops/s |
| 摊销增长 `Add` | 10,568,100 ns | 94,624,388.490 ops/s |

## TinyCC x86

编译器：`TinyCC`，参数：`-O2 -m32`。

| 路径 | 耗时 | 吞吐 |
|---|---:|---:|
| 预留 `Push/Pop` | 442,558,100 ns | 28,922,756.131 ops/s |
| 摊销增长 `Add` | 20,845,500 ns | 47,971,984.361 ops/s |

这些数字用于发现明显性能回退，不作为不同机器之间的绝对性能承诺。复测时应使用相同编译模式、参数和操作数量，并同时核对最终容量和 checksum。


	#if defined(_WIN32) || defined(_WIN64)
		// windows 方案
	#elif defined(__unix__) || defined(__unix) || defined(__linux__) || defined(__linux) || defined(__gnu_linux__)
		// unix、linux 方案
	#elif defined(__APPLE__) || defined(__MACH__)
		// macos 方案
	#elif defined(__ANDROID__)
		// android 方案
	#else
		// 其他平台方案
	#endif